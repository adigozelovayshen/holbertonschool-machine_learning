# Word Embeddings - Bag of Words

This directory contains implementations of Natural Language Processing (NLP) word embedding techniques.

## Files
* `0-bag_of_words.py`: A Python function `bag_of_words(sentences, vocab=None)` that creates a Bag of Words embedding matrix from scratch using `numpy` and `re`.
