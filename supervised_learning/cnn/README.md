This directory contains projects and implementations for Convolutional Neural Networks (CNN) as part of the machine learning curriculum.

## Tasks
* 0-conv_forward.py: Performs forward propagation over a convolutional layer of a neural network.
